CREATE DATABASE shortly

CREATE TABLE users ( id SERIAL PRIMARY KEY, name TEXT NOT NULL, password TEXT NOT NULL, email TEXT NOT NULL)

CREATE TABLE links (id SERTIAL PRIMARY KEY, userId NOT NULL, url TEXT NOT NULL, visitCount INTEGER NOT NULL)